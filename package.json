{
  "name": "boilerplate",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "copy": "cp -r shared/* frontend/ && cp -r shared/* backend/",
    "build": "npm run copy && cd frontend && npm run build && cd ../backend && npm run build && cd .. && npm run collect-build",
    "collect-build": "cp -r backend/dist/* dist/server && cp -r frontend/dist/* dist"
  },
  "keywords": [],
  "author": "Joshua Tazman Reinier",
  "license": "ISC"
}
